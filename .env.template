# ============================================================================
# Harbor 镜像下载工具 - 环境变量配置模板
# ============================================================================
# 使用方法：
# 1. 复制此文件为 .env: cp .env.template .env
# 2. 根据实际情况修改配置项
# 3. .env 文件不会被提交到 Git（已在 .gitignore 中配置）
# ============================================================================

# ----------------------------------------------------------------------------
# Flask 应用配置
# ----------------------------------------------------------------------------
# 安全密钥（生产环境请务必修改为随机字符串）
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# 调试模式（生产环境设置为 False）
FLASK_DEBUG=True

# Flask 环境（development/production/testing）
FLASK_ENV=development


# ----------------------------------------------------------------------------
# CORS 跨域配置
# ----------------------------------------------------------------------------
# 允许的跨域来源，多个用逗号分隔
# 开发环境可以使用 * 允许所有来源
# 生产环境建议指定具体的前端地址
CORS_ORIGINS=*
# 生产环境示例：
# CORS_ORIGINS=https://harbor-ui.example.com,https://app.example.com


# ----------------------------------------------------------------------------
# Harbor 测试连接配置（可选）
# ----------------------------------------------------------------------------
# 用于开发测试的 Harbor 仓库配置
# 注意：这些是测试用的配置，实际使用时通过 API 请求传入
TEST_HARBOR_URL=https://10.3.2.40
TEST_HARBOR_USERNAME=admin
TEST_HARBOR_PASSWORD=Harbor12345


# ----------------------------------------------------------------------------
# Docker 配置
# ----------------------------------------------------------------------------
# Docker 操作超时时间（秒）
DOCKER_TIMEOUT=600

# Docker Registry 是否允许不安全连接（开发环境可以设置为 True）
DOCKER_REGISTRY_INSECURE=True


# ----------------------------------------------------------------------------
# 文件存储配置
# ----------------------------------------------------------------------------
# 临时文件目录（相对于项目根目录）
UPLOAD_FOLDER=temp

# 下载文件目录（相对于项目根目录）
DOWNLOAD_FOLDER=downloads

# 最大文件上传大小（字节，默认 16GB）
MAX_CONTENT_LENGTH=17179869184


# ----------------------------------------------------------------------------
# 日志配置
# ----------------------------------------------------------------------------
# 日志级别：DEBUG/INFO/WARNING/ERROR/CRITICAL
LOG_LEVEL=INFO

# 日志文件路径（相对于项目根目录）
LOG_FILE=logs/app.log

# 单个日志文件最大大小（字节，默认 10MB）
LOG_MAX_BYTES=10485760

# 保留的日志文件数量
LOG_BACKUP_COUNT=5


# ----------------------------------------------------------------------------
# Harbor API 配置
# ----------------------------------------------------------------------------
# Harbor API 版本
HARBOR_API_VERSION=v2.0

# Harbor API 请求超时时间（秒）
HARBOR_REQUEST_TIMEOUT=30


# ----------------------------------------------------------------------------
# 服务器配置
# ----------------------------------------------------------------------------
# 服务监听地址
SERVER_HOST=0.0.0.0

# 服务监听端口
SERVER_PORT=5000

# 是否启用多线程
SERVER_THREADED=True


# ----------------------------------------------------------------------------
# 数据库配置（预留，如果后续需要数据库）
# ----------------------------------------------------------------------------
# DATABASE_URL=sqlite:///harbor.db
# DATABASE_POOL_SIZE=5
# DATABASE_POOL_TIMEOUT=30


# ----------------------------------------------------------------------------
# Redis 配置（预留，如果后续需要缓存）
# ----------------------------------------------------------------------------
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=
# CACHE_TTL=3600


# ----------------------------------------------------------------------------
# 认证配置（预留，如果后续需要用户认证）
# ----------------------------------------------------------------------------
# JWT_SECRET_KEY=your-jwt-secret-key
# JWT_ACCESS_TOKEN_EXPIRES=3600
# JWT_REFRESH_TOKEN_EXPIRES=86400


# ----------------------------------------------------------------------------
# 监控配置（预留）
# ----------------------------------------------------------------------------
# ENABLE_METRICS=False
# METRICS_PORT=9090